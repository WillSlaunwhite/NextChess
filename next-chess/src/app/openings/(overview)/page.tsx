import { Button } from "@/app/ui/button";

export default async function Page() {
    return (
        <main>
            <Button children={undefined} placeholder={undefined} onPointerEnterCapture={undefined} onPointerLeaveCapture={undefined} ></Button>
        </main>
    )
}